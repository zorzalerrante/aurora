from django.conf.urls import patterns, url

from birds import views

urlpatterns = patterns('',
    url(r'^utils/common-place-names/$', views.place_names, name='common-place-names'),
    url(r'^/$', views.home, name='home'),
)
